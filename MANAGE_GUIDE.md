# Strikeblade 管理页面使用说明

## 管理入口

本地开发环境：

```text
http://127.0.0.1:5000/manage
```

线上部署后：

```text
https://你的 Render 域名/manage
```

该入口不会显示在网站公开导航中，需要管理员手动输入地址访问。

## 唯一管理员账号

```text
用户名：uniquede
密码：350234
```

登录后可进入产品管理页面。管理完成后，点击右上角“退出登录”。

## 新增货物

进入 `/manage` 后，在左侧“产品管理”表单填写货物信息：

- 产品名称：必填，显示在首页产品卡和详情页标题中。
- 刀剑种类：使用下拉列表选择，包含单手、双手、综合。
- 刀剑来源：使用下拉列表选择，包含欧洲、亚洲、亚欧大陆风格。
- 时期：使用下拉列表选择，例如古代、中世纪、近世、近代、现代、综合时期。
- 状态：可选正在销售、开团、售罄、隐藏。
- 钢材：例如花纹钢、1095 碳钢、弹簧钢。
- 长度：例如 98 cm。
- 价格/报价说明：例如咨询报价、私信报价、定金开团。
- 简短介绍：用于详情页顶部说明。
- 详细说明：用于详情页正文，可写材质、工艺、用途和注意事项。
- 产品主图：显示在首页产品卡和详情页主视觉。
- 详情图：可一次上传多张，显示在产品详情页下方。

如需让产品显示在首页“正在销售 / 开团”区域，请勾选：

```text
展示在首页“正在销售 / 开团”
```

同时产品状态必须是：

```text
正在销售
```

或：

```text
开团
```

状态为“售罄”或“隐藏”的产品不会显示在首页销售区。

## 编辑产品

右侧“已有产品”区域会列出全部产品。

可执行：

- 编辑：重新打开产品表单并修改信息。
- 查看详情页：预览用户看到的产品详情页。
- 删除：移除该产品记录。

编辑时，如果不重新上传主图，系统会保留原主图。

详情图可以继续追加上传。若要移除已有详情图，勾选对应图片下方的“移除此图”，然后保存修改。

## 新增和编辑文章/研究

前台导航中已经新增：

```text
文章/研究
```

位置在：

```text
首页｜购买须知｜定制｜文章/研究｜联系
```

进入 `/manage` 后，页面下方有“文章 / 研究”管理区。

文章表单包含：

- 文章标题：必填。
- 状态：草稿或发布。
- 封面图：显示在文章列表和详情页顶部。
- 摘要：显示在文章列表和文章详情页标题下方。
- 正文编辑器：用于撰写文章内容。

正文编辑器支持：

- 正文、段落标题、小标题切换。
- 小号、正文、大号、特大字号。
- 下划线。
- 清除格式。
- 在文字之间插入图片。

插入正文图片的方法：

1. 将光标放到文章正文中需要插入图片的位置。
2. 点击“插入图片”。
3. 选择本地图片。
4. 图片上传完成后会自动插入到正文光标位置。

文章状态说明：

- 草稿：只保存在后台，不显示到前台文章列表。
- 发布：显示在 `/research` 文章/研究列表页。

已有文章可以在右侧文章列表中执行：

- 编辑。
- 查看文章。
- 删除。

文章数据保存到：

```text
data/articles.json
```

文章图片保存到：

```text
static/uploads/articles/
```

## 首页展示规则

首页只负责展示主要标题、介绍、系列入口，以及后台标记出来的销售产品。

首页产品区的数据来自：

```text
data/products.json
```

后台上传的图片会保存到：

```text
static/uploads/products/
```

首页不会直接手写固定产品，产品上新和编辑都应通过 `/manage` 完成。

## 全部产品页面

前台导航中已经新增：

```text
全部产品
```

位置在：

```text
首页｜购买须知｜全部产品｜定制｜文章/研究｜联系
```

页面地址：

```text
/products
```

用户可以在全部产品页面按三组列表筛选：

```text
刀剑种类：单手 / 双手 / 综合
刀剑来源：欧洲 / 亚洲 / 亚欧大陆风格
时期：古代 / 中世纪 / 近世 / 近代 / 现代 / 综合时期
```

后台产品编辑中的“刀剑种类 / 刀剑来源 / 时期”会影响该页面的筛选结果。状态为“隐藏”的产品不会出现在全部产品页面。

## Render 部署注意事项

当前项目启动命令：

```text
gunicorn app:app
```

建议在 Render 环境变量中设置：

```text
MANAGE_USERNAME=uniquede
MANAGE_PASSWORD=350234
APP_SECRET_KEY=一串随机长密钥
```

注意：Render 普通文件系统不适合长期保存上传图片。正式上线后，如果需要长期保存后台上传图片，建议配置 Render Disk 或接入云存储。
